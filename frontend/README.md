# MaxTasks web client

The frontend is a React and TypeScript application built with Vite. It is the web and Android PWA client for MaxTasks.

## Local commands

From `frontend/`:

```powershell
npm install
npm run dev
npm run build
npm run lint
```

The current screen is the initial application shell. Task-list functionality is planned in the MVP milestone.
